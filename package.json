{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "test": "jest",
    "pre-deploy": "cross-env PORT=5000 pm2 start server.js --name temp-app && timeout /t 2 && pm2 delete temp-app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "node-cron": "^4.6.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^30.5.1",
    "pm2": "^7.0.4"
  }
}
